<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <link rel="stylesheet" href="css/global.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
    <title> Website Name </title>
</head>
<body>

    <%- include("../partials/header.ejs") %>

    <div class="challenges">

        <div class="container">

            <div class="card" style="width: auto;">
                <div class="card-body">
                    <h5 class="card-title"> Lazy Admin </h5>
                    <small> 5 Points - Easy </small>
                    <p class="card-text">The credentials are somewhere on the page for this lazy admin. </p>
                    <a href="/lazy-admin" class="btn btn-primary"> Open </a>
                    <a id="submit" class="btn btn-secondary"> Submit Flag </a>


                    <% if (status == 452) { %>
                        <div class="alert alert-danger flag-card-message" role="alert">
                            Incorrect Flag!
                        </div>
                    <% } %>

                    <% if (status == 400) { %>
                        <div class="alert alert-danger flag-card-message" role="alert">
                            Something went wrong, please try again!
                        </div>
                    <% } %>

                    <% if (status == 227) { %>
                        <div class="alert alert-success flag-card-message" role="alert">
                            Correct, well done!
                        </div>
                    <% } %>
                    
                    
                    <form action="/" method="POST" id="flag-submission">
                        <div class="input-group mb-3">
                            <div class="input-group-prepend">
                              <span class="input-group-text" id="inputGroup-sizing-default"> Flag </span>
                            </div>
                            <input type="text" name="flag" class="flag-input" aria-label="Default" aria-describedby="inputGroup-sizing-default">
                            <input type="text" name="challenge" value="Lazy Admin" hidden>
                        </div>
                        <input type="submit" class="btn btn-primary" value="Submit Flag">
                        
                    </form>
                </div>
              </div>

        </div>

    </div>

    <script>
    
    $( "#submit" ).click(function() {
        $( "#flag-submission" ).toggle( "slow", function() {
            // Animation complete.
        });
    });
    </script>

    <%- include("../partials/footer.ejs") %>

</body>
</html>